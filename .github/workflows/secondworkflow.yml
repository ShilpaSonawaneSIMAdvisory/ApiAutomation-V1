name: first workflow

on:
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print greeting
        run: echo "Hello World"

      - name: Say goodbye again
        run: echo "Bye - Done"
